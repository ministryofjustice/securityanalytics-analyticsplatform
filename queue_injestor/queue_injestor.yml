function:
  handler: queue_injestor.queue_injestor.injest
  name: ${opt:stage, '${env:USERNAME}'}-${self:service}-injestor
  role: injestorRole
  environment:
    REGION: ${self:provider.region}
    STAGE: ${self:provider.stage}
    APP_NAME: ${self:custom.app-name}
    TASK_NAME: ${self:custom.plugin-name}
  layers:
    - ${ssm:/${self:custom.app-name}/${opt:stage, '${env:USERNAME}'}/lambda/layer/utils, ssm:/${self:custom.app-name}/dev/lambda/layer/utils}
  events:
    - sqs:
        arn: ${ssm:/${self:custom.app-name}/${self:provider.stage}/analytics/elastic/injest_queue/arn}
        batchSize: 1
        enabled: true

role:
  Type: AWS::IAM::Role
  Properties:
    Path:
      # Not allowed '-' characters in a path
      Fn::Join:
        - ""
        - Fn::Split:
            - "-"
            - /${self:custom.app-name}/${opt:stage, '${env:USERNAME}'}/${self:service}/analytics/elastic/injestor/
    RoleName: ${opt:stage, '${env:USERNAME}'}-${self:service}-elastic-injestor
    AssumeRolePolicyDocument:
      Version: '2012-10-17'
      Statement:
        - Effect: Allow
          Principal:
            Service:
              - lambda.amazonaws.com
          Action: sts:AssumeRole
    Policies:
      - PolicyName: ${opt:stage, '${env:USERNAME}'}-${self:service}-elastic-injestor
        PolicyDocument:
          Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action:
                - logs:CreateLogGroup
                - logs:CreateLogStream
                - logs:PutLogEvents
              Resource:
                - 'Fn::Join':
                    - ':'
                    - - 'arn:aws:logs'
                      - Ref: 'AWS::Region'
                      - Ref: 'AWS::AccountId'
                      - 'log-group:/aws/lambda/*:*:*'
            - Effect: Allow
              Action:
                - sqs:DeleteMessage
                - sqs:ReceiveMessage
                - sqs:GetQueueAttributes
              Resource: ${ssm:/${self:custom.app-name}/${self:provider.stage}/analytics/elastic/injest_queue/arn}